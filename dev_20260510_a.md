---
name: AetherOS 开发进度总结
description: N.O.V.A Aether OS 浏览器端操作系统项目的所有开发内容、架构、已知问题和待办事项
type: project
originSessionId: 0442dfb5-fa64-47c3-a3d2-d30fc2c3e09c
---
# N.O.V.A Aether OS — 开发总结

**项目路径**：`/home/emyake/projects/AetherOS`
**开发文档**：`webos-dev-doc.md`
**启动方式**：`./start.sh`（端口 8411）
**停止方式**：`./stop.sh`

---

## 一、项目架构（22 个文件）

```
AetherOS/
├── start.sh / stop.sh          # 启停脚本
├── server.py                    # FastAPI 后端
├── requirements.txt             # Python 依赖
│
├── static/
│   ├── index.html               # OS 主页面
│   ├── core/
│   │   ├── os.css               # 全局样式（字体、glassmorphism、动画）
│   │   ├── os.js                # 核心 OS（窗口管理/Dock/侧边栏/菜单/右键菜单）
│   │   └── window.js            # 窗口类（拖拽/缩放/最大化动画/吸附）
│   │
│   └── apps/
│       ├── files/files.js       # 文件管理器
│       ├── ide/ide.js           # IDE（Monaco Editor + 多标签 + 自动保存）
│       ├── terminal/terminal.js # 终端（xterm.js + WebSocket PTY）
│       ├── agent/agent.js       # Agent（对话 + 工具面板 + 侧边栏联动）
│       ├── browser/browser.js   # 浏览器（URL 启动器，window.open 外部打开）
│       ├── monitor/monitor.js   # 模型监控
│       └── settings/settings.js # 设置（Agent/工具/MCP/上下文/外观/快捷键）
│
├── agent/
│   ├── engine.py                # Agent 循环引擎（LLM 调用 + 工具执行）
│   ├── context.py               # 上下文管理（滑动窗口/摘要压缩）
│   ├── config.yaml              # Agent 配置
│   └── tools/
│       ├── registry.py          # 工具注册中心（内置 + 热加载 + MCP）
│       └── custom/              # 自定义工具目录
│
├── fonts/                       # 本地字体
│   ├── terminal-f4/Terminal F4.ttf
│   ├── iosevka-charon/IosevkaCharon-*.ttf
│   ├── LXGWWenKai-Regular.ttf
│   └── 造字工房朗宋（非商用）常规体.otf
│
└── svg/                         # SVG 图标（6 个）
    ├── files.svg, ide.svg, terminal.svg
    ├── agent.svg, monitor.svg, settings.svg
    ├── browser.svg（自绘）
    └── icon-prompts.md          # 图标生成提示词文档
```

---

## 二、UI 设计规范

### 视觉风格
- **主题**：Cyber-noir 深色科技风
- **主色**：`#00e5ff`（电光蓝），辅色 `#6c5ce7`（靛紫）
- **背景**：`#06080f` 深蓝黑 + 微妙网格
- **玻璃效果**：亮色半透明 `rgba(210,235,255,0.10)` + `blur(32px) saturate(1.8)`

### 字体配置（关键：Western 首位，中文末位）
```css
--font-mono:    'Terminal F4', 'Iosevka Charon', 'LXGW WenKai', monospace;
--font-display: 'Iosevka Charon', 'LXGW WenKai', sans-serif;
--font-body:    'Iosevka Charon', 'LXGW WenKai', sans-serif;
--font-code:    'Iosevka Charon', 'Terminal F4', 'LXGW WenKai', monospace;
```
**Why:** 中文字体放在末位，浏览器对 CJK 字符自动回退，Latin 字符使用 Iosevka Charon。

### 系统设计元素
- ▽ 倒三角形：系统 Logo
- ⬡ 被切割的正六边形：设计元素
- ○ 圆形：设计元素
- 这三者定义整体视觉语言，不是直接做 UI 组件

---

## 三、核心功能状态

### 已完成
- **窗口管理**：拖拽、边缘缩放、最小化（带动画）、最大化（JS 动画 + snap-to-full）、关闭、左右半屏吸附、z-index 堆叠
- **菜单栏**：▽ 系统图标 + 6 个下拉菜单 + 实时时钟
- **侧边栏**：独立圆角磨砂玻璃容器，`translateX` 滑入动画，不推开桌面
  - 模型调用面板（glassmorphism 卡片）
  - Agent 运行面板（状态/时间/token）
  - 窗口预览列表（Alt+Tab 风格，点击切换/关闭）
- **Dock 栏**：浮动定位居中，`inline-flex` 自适应宽度，32px SVG 图标
  - 弹簧放大 `scale(1.45)` + 邻居联动 + 弹跳动画
  - 标签 tooltip 风格弹出在上方
  - 运行指示器脉冲光晕
- **桌面右键菜单**：禁用浏览器默认右键，弹出 Windows 风格菜单（新建文件/文件夹/打开应用）
- **启动动画**：▽ 六边形旋转 + 进度条 + 状态文字
- **SVG 图标**：5 个专业生成 + 1 个自绘 browser.svg

### 各应用
- **文件管理器**：树形侧栏 + 面包屑 + 双击打开（联动 IDE）
- **IDE**：Monaco Editor CDN 加载 + 多标签 + 文件树 + 自动保存 + LSP 状态栏
- **终端**：xterm.js CDN + WebSocket PTY（需后端 ptyprocess）
- **Agent**：对话面板 + 工具调用追踪 + 终端输出 + 侧边栏 Agent 面板联动
- **浏览器**：URL 启动器，`window.open` 外部打开（无内嵌代理）
- **监控**：统计卡片 + 调用历史表 + Agent 状态列表
- **设置**：7 个设置页面（通用/Agent/工具/MCP/上下文/外观/快捷键）

---

## 四、已知问题与未完成项

### 浏览器应用
- 只能外部打开，无法在 OS 内嵌浏览（iframe 被 X-Frame-Options/CSP 阻止）
- 曾尝试 Python 代理和 Node.js 反向代理，均未成功或被用户否决
- **待办**：需要找到可行的内嵌浏览方案

### Agent 引擎
- 后端 engine.py 已写好，但未实际测试 LLM 调用
- 需要用户配置 API Key 和模型才能工作
- MCP 接入和热加载工具未测试

### 终端
- 依赖 ptyprocess，需要在 Linux 环境下测试
- xterm.js 从 CDN 加载，可能有网络问题

### 其他
- 侧边栏自定义面板：预留接口，未实现
- LSP 代理：代码已写，未测试
- 窗口状态持久化：未实现
- 中文字体 LXGW 文楷：用户要求"更加实用优雅"，当前方案待确认

---

## 五、用户偏好记录

- **语言**：中文母语，所有沟通使用中文
- **设计**：不喜欢苹果风格，窗口控制按钮在右上角，去苹果化
- **字体**：使用本地字体（Terminal F4 / Iosevka Charon / LXGW 文楷），不使用 Google Fonts
- **图标**：使用 SVG，不用 emoji
- **调试**：不在浏览器中自行调试，调用用户测试并返回结果
- **效率**：节约 token，并发工具调用，最少轮次

**Why:** 这些偏好影响所有后续开发决策。
**How to apply:** 每次新会话开始时参考此文件，避免重复已踩过的坑。
