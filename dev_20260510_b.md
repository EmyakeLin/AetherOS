# AetherOS 开发日志 — 2026/05/10 (B)

## 一、前端主题系统

### 1.1 米色亮色主题

新增 `[data-theme="light"]` 主题，通过 CSS 变量覆盖实现：

- **配色**：米色背景 `#d8cfc2` → `#ece6db`，深金强调色 `#b8860b`，厚重阴影
- **覆盖范围**：菜单栏、侧边栏、Dock、窗口、弹窗、桌面网格、右键菜单、滚动条、选中色、启动画面
- **切换机制**：`os.setTheme(theme)` 操作 `document.documentElement` 的 `data-theme` 属性，`localStorage` 持久化
- **设置界面**：设置 → 外观 → 主题下拉框联动 `os.setTheme()`

### 1.2 启动画面主题适配

- SVG 六边形和三角形使用 `var(--accent)` 自动跟随主题色
- boot-text 的 `text-shadow` 使用 `var(--accent-glow)` 替代硬编码青色
- 进度条、状态文字均有亮色主题覆盖样式
- 在 `<head>` 中内联脚本读取 `localStorage` 提前设置 `data-theme`，避免启动画面闪烁

---

## 二、Dock 栏

- **位置上调**：`bottom: 12px` → `bottom: 36px`
- **动态化**：删除 `index.html` 中硬编码的 7 个 Dock 项，改为由 `_buildDock()` 从应用清单动态生成

---

## 三、SVG 图标替换

将所有 Unicode 箭头/三角形/符号替换为内联 SVG，涉及 8 个文件约 20 处：

| 文件 | 替换内容 |
|------|----------|
| `os.js` | 新增 `SVG` 工具对象（20+ 图标函数），替换 agent 面板 ◇、窗口预览 ✕、右键菜单 ↻ |
| `index.html` | 启动画面 ▽、系统按钮 ▽、侧边栏 4 个节图标 ◈⬡◇○、调用记录 ▾、弹窗关闭 ✕ |
| `window.js` | 窗口控制按钮 ─□✕ |
| `browser.js` | 导航按钮 ◀▶▸⌂、主页 logo ▽ |
| `agent.js` | 发送按钮 ▶ |
| `ide.js` | 就绪提示 ▸、标签关闭 ✕、LSP 状态 ○ |
| `monitor.js` | 刷新按钮 ↻ |

---

## 四、文件管理器增强 (`files.js` 完全重写)

- **列表/图标视图切换**：工具栏新增两个按钮，图标视图以 grid 展示大图标
- **隐藏文件过滤**：眼睛按钮切换显示以 `.` 开头的文件，状态栏显示被过滤数量
- 文件夹和文件图标全部使用 SVG（替换 emoji）
- 同步适配亮色主题

---

## 五、IDE 应用增强

### 5.1 Explorer 侧栏折叠

- 标题栏按钮移到标签栏右侧（与 Run、面板切换按钮并排），折叠后按钮仍然可见

### 5.2 底部面板可调高度

- 面板顶部新增 5px 拖拽手柄（带居中圆点指示器），拖动范围 60px–500px
- 右上角新增按钮可完全隐藏/显示底部面板

### 5.3 Run 按钮

- 右上角三角形播放按钮，根据文件扩展名自动选择运行命令
- 通过 `POST /api/exec` 执行，结果输出到底部"输出"面板
- 运行中按钮带脉冲动画

---

## 六、动态应用系统

### 6.1 应用清单 (`app.json`)

每个应用文件夹含 `app.json` 定义元数据：

```json
{
    "id": "my-app",
    "title": "我的应用",
    "icon": "/apps/my-app/icon.svg",
    "emoji": "🎯",
    "entry": "my-app.js",
    "version": "1.0.0",
    "author": "user",
    "description": "应用描述",
    "dock": true,
    "width": 800,
    "height": 500
}
```

### 6.2 后端 API (`server.py`)

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/apps` | GET | 扫描 `static/apps/` 返回所有应用清单 |
| `/api/apps` | POST | 创建新应用（生成文件夹 + app.json + 入口 JS 模板） |
| `/api/apps/{id}` | PUT | 更新应用元数据 |
| `/api/apps/{id}` | DELETE | 删除应用文件夹 |
| `/api/exec` | POST | 执行命令，返回 `{stdout, stderr, exit_code}` |

### 6.3 前端改造 (`os.js`)

- 删除硬编码 `AppIcons`，新增 `AppManifests` 从 API 获取
- `_loadApps()` — boot 时调用 `/api/apps`，逐个 `<script>` 动态加载 entry JS
- `_buildDock()` — 从 manifests 动态生成 Dock 项
- `openApp()` 改为 async，未注册时自动按需加载脚本
- `getAppIcon()` — 优先使用 manifest.icon，回退到 emoji

### 6.4 index.html

- 删除 7 个硬编码 `<script>` 标签，仅保留 `window.js` 和 `os.js`
- Dock 容器留空由 JS 填充

### 6.5 设置 → 应用管理

- 列出所有已安装应用（区分内置/自定义）
- 创建新应用表单（ID、标题、图标、描述）
- 删除自定义应用（内置应用不可删除）
- 操作后自动刷新 Dock

### 6.6 内置应用 app.json

为 7 个内置应用各创建 `app.json`：files、ide、terminal、agent、browser、monitor、settings

### 6.7 应用开发文档

创建 `APP_DEV.md`，涵盖：
- 快速开始（3 种创建方式）
- app.json 字段说明
- Factory 函数 API（container、win、os 三个参数）
- 后端 API 端点一览
- CSS 变量参考
- 完整计数器应用示例
- 应用生命周期说明

---

## 七、右键菜单改造

- 仅在桌面背景（`#desktop`、`#desktop-canvas`、`.desktop-grid`）上右击才弹出系统菜单
- 窗口内右击不再触发系统菜单，改为检查 `win._contextMenuHandler`
- 应用可通过 `win.setContextMenu((x, y, target) => {...})` 注册自定义右键行为

---

## 八、桌面动画背景

### 8.1 暗色主题

- `<canvas>` 绘制 14 个彩色泛光圆形，半径 60–200px
- 颜色：青、紫、热粉、翡翠绿、金、天蓝
- 透明度 0.06–0.18，带呼吸灯效果
- 径向渐变绘制：中心全亮 → 边缘完全透明，产生泛光/光晕效果
- CSS `mask-image` 径向渐变：中间最可见，向四周渐隐
- 圆形缓慢漂移（速度 0.25），窗口边缘自动循环

### 8.2 亮色主题

- Canvas 和动画完全跳过（`display: none`），零性能开销

---

## 九、Bug 修复

- **pyyaml 重复安装提示**：`start.sh` 中 `import pyyaml` 修正为 `import yaml`
- **python-multipart 缺失**：加入 `requirements.txt` 并安装
- **AppIcons 引用未清除**：`openApp()` 中残留 `AppIcons[appId]` 改为使用 `AppManifests`
- **Canvas scale 累乘**：`ctx.scale()` 改为 `ctx.setTransform()` 防止 resize 时变换矩阵累乘
- **Canvas 尺寸为零**：CSS 添加 `width: 100%; height: 100%` 确保 `offsetWidth` 正确返回像素值

---

## 修改文件清单

| 文件 | 改动类型 |
|------|----------|
| `static/core/os.css` | 亮色主题变量、Boot 主题适配、Dock 位置、Canvas 样式 |
| `static/core/os.js` | SVG 工具、主题管理、动态应用加载、Canvas 动画、右键菜单 |
| `static/core/window.js` | 窗口控制 SVG、`setContextMenu()` API |
| `static/index.html` | 内联主题脚本、Canvas 元素、删除硬编码脚本/Dock |
| `static/apps/files/files.js` | 完全重写：视图切换、隐藏文件、SVG 图标 |
| `static/apps/ide/ide.js` | Explorer 折叠、面板调整、Run 按钮 |
| `static/apps/browser/browser.js` | SVG 替换 |
| `static/apps/agent/agent.js` | SVG 替换 |
| `static/apps/monitor/monitor.js` | SVG 替换 |
| `static/apps/settings/settings.js` | 主题切换、应用管理页面 |
| `static/apps/*/app.json` | 7 个内置应用清单（新增） |
| `server.py` | `/api/apps` CRUD、`/api/exec` 端点 |
| `start.sh` | pyyaml 导入名修正、python-multipart 检查 |
| `requirements.txt` | 添加 `python-multipart` |
| `APP_DEV.md` | 应用开发文档（新增） |
